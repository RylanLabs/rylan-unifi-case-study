# TODO: clarify: insert exact dnsmasq proxy-DHCP PXE config from Expansion Blueprint v2
# Must reflect:
# - ProxyDHCP only (no L3 DHCP), serve boot filename for BIOS/UEFI
# - Listen/interface constraints per blueprint
# - IP alignment for 10.0.30.10 on VLAN 30 sub-interface
